import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    content: {
        minHeight: "100%", // Altura mínima de la pantalla
        backgroundColor: "#f2f2f2", // Color de fondo de la pantalla
    },
    btnStyles: {
        marginTop: 30,
        paddingVertical: 10,
        borderRadius: 0,
        backgroundColor: "#fff",
        borderTopWidth: 1,
        borderTopColor: '#e3e3e3',
        borderBottomWidth: 1,
        borderBottomColor: "#e3e3e3",
    },

    btnTextStyles: {
        color: "#00a680",
    }
});
