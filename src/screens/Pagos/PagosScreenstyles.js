import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
        backgroundColor: "#FFF",
    },
    titulo: {
        fontSize: 20,
        fontWeight: "bold",
        marginBottom: 20,
    },
    inputContainer: {
        marginBottom: 10,
    },
    etiqueta: {
        fontSize: 16,
    },
    input: {
        borderWidth: 1,
        borderColor: "#ECECEC",
        borderRadius: 5,
        paddingHorizontal: 10,
        height: 40,
        fontSize: 16,
    },
});
